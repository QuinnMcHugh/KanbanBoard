# Optional. Base URL of the API, e.g. https://api.example.com (no trailing slash).
# Leave unset for local dev — requests stay relative and are proxied by Vite
# (see vite.config.ts) to http://localhost:5001. Required at build time for any
# deploy where the built client isn't served from the same origin as the API.
VITE_API_URL=
